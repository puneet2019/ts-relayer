# CircleCI configuration deprecated. CI has been migrated to GitHub Actions in .github/workflows.
# This file is intentionally left minimal to avoid triggering CircleCI jobs.
version: 2.1
jobs:
  noop:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo "CI moved to GitHub Actions."
workflows:
  version: 2
  build:
    jobs:
      - noop
